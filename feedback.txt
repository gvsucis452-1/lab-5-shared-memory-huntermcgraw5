1. Output `b` is technically the first address past the shared memory space.
2. What specifically does `IPC_PRIVATE` tell you about how the shared memory space is created? 

------

The rece condition appears to be gone; but, I don't understand 
what you are using the pipes for. 